<div class="mac-wrap">
  <div class="mac-panel mac-before">
    <div class="mac-title" style="color:var(--mbi)">BEFORE HEATING — metastable</div>
    <div class="mac-viz">
      <div style="width:30px;height:12px;background:var(--mbi);border-radius:3px;opacity:.82;transform:rotate(14deg)"></div>
      <div style="width:30px;height:12px;background:var(--mbi);border-radius:3px;opacity:.82;transform:rotate(-18deg)"></div>
      <div style="width:30px;height:12px;background:var(--mbi);border-radius:3px;opacity:.82;transform:rotate(6deg)"></div>
      <div style="width:30px;height:12px;background:var(--mbi);border-radius:3px;opacity:.82;transform:rotate(-11deg)"></div>
      <div style="width:30px;height:12px;background:var(--mbi);border-radius:3px;opacity:.82;transform:rotate(20deg)"></div>
      <div style="width:30px;height:12px;background:var(--mbi);border-radius:3px;opacity:.82;transform:rotate(-7deg)"></div>
      <div style="width:30px;height:12px;background:var(--mbi);border-radius:3px;opacity:.82;transform:rotate(16deg)"></div>
      <div style="width:30px;height:12px;background:var(--mbi);border-radius:3px;opacity:.82;transform:rotate(-4deg)"></div>
    </div>
    <div class="mac-sub">Disordered stacking — large aggregates</div>
    <div class="mac-abs">Absorbs ~530nm</div>
    <div class="mac-badge" style="background:var(--mbi)">Appears VIOLET</div>
    <div class="mac-note">Bromine: intact ✓ &nbsp; C=C: intact ✓</div>
  </div>

  <div class="mac-arrow">⬇ 60°C in water · irreversible ⬇</div>

  <div class="mac-panel mac-after">
    <div class="mac-title" style="color:var(--ind)">AFTER 60°C in water — stable</div>
    <div class="mac-viz">
      <div style="width:12px;height:32px;background:var(--ind);border-radius:3px;opacity:.88"></div>
      <div style="width:12px;height:32px;background:var(--ind);border-radius:3px;opacity:.88"></div>
      <div style="width:12px;height:32px;background:var(--ind);border-radius:3px;opacity:.88"></div>
      <div style="width:12px;height:32px;background:var(--ind);border-radius:3px;opacity:.88"></div>
      <div style="width:12px;height:32px;background:var(--ind);border-radius:3px;opacity:.88"></div>
      <div style="width:12px;height:32px;background:var(--ind);border-radius:3px;opacity:.88"></div>
      <div style="width:12px;height:32px;background:var(--ind);border-radius:3px;opacity:.88"></div>
      <div style="width:12px;height:32px;background:var(--ind);border-radius:3px;opacity:.88"></div>
    </div>
    <div class="mac-sub">Ordered columns — smaller aggregates</div>
    <div class="mac-abs">Absorbs ~620nm</div>
    <div class="mac-badge" style="background:var(--ind)">Appears BLUE</div>
    <div class="mac-note">Bromine: intact ✓ &nbsp; C=C: intact ✓</div>
  </div>
</div>

<style>
  .mac-wrap {
    display: flex;
    flex-direction: column;
    align-items: center;
    max-width: 420px;
    margin: 0 auto;
  }
  .mac-panel {
    width: 100%;
    border-radius: var(--radius);
    padding: 1rem;
    text-align: center;
  }
  .mac-before {
    background: #f5f0ff;
    border: 2px solid var(--mbi);
  }
  .mac-after {
    background: #eff6ff;
    border: 2px solid var(--ind);
  }
  .mac-title {
    font-family: var(--ff-disp);
    font-size: .95rem;
    font-weight: 600;
    margin-bottom: .7rem;
  }
  .mac-viz {
    display: flex;
    flex-wrap: wrap;
    gap: 3px;
    justify-content: center;
    height: 56px;
    align-items: center;
    align-content: center;
    margin: .6rem 0;
  }
  .mac-sub {
    font-family: var(--ff-mono);
    font-size: .7rem;
    color: var(--muted);
  }
  .mac-abs {
    font-family: var(--ff-body);
    font-size: .8rem;
    color: var(--muted);
    margin: .3rem 0;
  }
  .mac-badge {
    height: 22px;
    border-radius: 4px;
    margin-top: .5rem;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-family: var(--ff-body);
    font-size: .8rem;
  }
  .mac-note {
    font-family: var(--ff-mono);
    font-size: .65rem;
    color: var(--muted);
    margin-top: .4rem;
  }
  .mac-arrow {
    font-size: 1.1rem;
    color: var(--muted);
    padding: .4rem 0;
    font-family: var(--ff-body);
  }
</style>
